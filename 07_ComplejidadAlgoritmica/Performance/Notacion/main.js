// Notación Big-O en complejidad temporal

let bar = 'test'	// O(1) Constante
if() {}		// O(1) Constante
for() {}		// O(n) Exponencial
while() {}	// O(n) Exponencial
for() { for() {} }// O(n^2) Cuadratico
// Big-O complejidad algoritmica espacial

let bar = 'test'  // 0(1) Constante
if () {}               // 0(1) Constante
for () {}             // 0(1) Constante
let resultado = [1,2,...,n]  // 0(n) Exponencial
let dimensional = [[2,4],[6,8],[10,12]] //0(n^2) Cuadratico